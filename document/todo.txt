1、向下翻页，剩下区域不足一页显示，则自动提取下一章数据(OK)；
2、向上翻页也是类似(OK)；
3、解析出的内容，去掉div标签，只保留内容(OK)；
4、根据阅读位置，自动更新书签(OK)；
5、在工具栏增加按钮，可以将书籍加入到书架；
6、增加删除书籍功能(OK)；
7、可以自动刷新书架，知道哪些书更新了(OK)；
8、有书籍更新，则工具栏按钮有显示；
9、解决返回结果乱码的问题(OK)；
10、可以正常显示目录(OK)；
11、显示目录时不修改书签(OK)；
12、可以根据内容过滤，去掉无用内容(OK)；
13、只保留一定范围的内容，多余内容删除，减少内存占用（因导致滚动异常，挂起该需求）；
14、增加添加书籍功能(OK)；
15、增加数据库备份功能；
16、按照更新日期排序功能(OK)；
17、按照书名、作者查找过滤功能；
18、自定义页面解析规则；
19、修改自动提取下一页的算法，提前提取(导致书签异常，挂起)；
20、书籍排序(OK)；
21、将显示部分全部封装到BookView对象内部(OK)；
22、点击标题不弹出阅读界面(OK)；
23、把应用放到工具栏上(OK)；
24、书架页面增加一个工具栏(OK)；
25、后台自动把常看书籍的书签页载入，加快速度；
26、对数据库使用缓存，变异步操作为同步操作(OK)；
27、按增加书籍按钮，弹出的输入框内容要清空(OK)；
28、图标显示更新的书籍数量(OK)；
29、增加云同步功能；
30、目录文件保存在本地(OK)；
31、添加书籍时，根据链接自动搜索书名和作者(OK)；
32、仿照百度音乐，鼠标移动到所在行上才显示删除图标(OK)；
33、建立GitHub帐号，代码放到上面(OK)；
34、支持多语言；
